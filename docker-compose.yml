services:
  coinmate-bot:
    build: .
    container_name: coinmate-bot
    restart: unless-stopped
    ports:
      - "8085:3000"
    volumes:
      - ./data:/app/data
      - ./bot_logs:/app/logs
    environment:
      - TZ=Europe/Prague
